package com.aelithron.teamManager;

import org.bukkit.event.Listener;

public class TeamListener implements Listener {
    private TeamManager plugin;
    public TeamListener(TeamManager plugin) {
        this.plugin = plugin;
    }

}
